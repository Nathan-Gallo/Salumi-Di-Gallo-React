import React from 'react';

const Recipes = (props: any) => {
    let allRecipes: Array<object> = props

    // find function for run on page load
    
    return (
        <h1>under construction</h1>
    )
}

export default Recipes;